[project]
name = "kalman-filter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
]

[project.scripts]
kalman-filter = "kalman_filter.simulation:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
